## Краткое объяснение

В продакшене **Nginx** редко используется просто для отдачи статических файлов. Его главная роль — **Reverse Proxy** (обратный прокси). Он принимает запросы из интернета и перенаправляет их на внутренние сервисы (например, на приложение на Python или Node.js), скрывая их реальные IP и добавляя уровень безопасности и балансировки.

> [!note] Reverse Proxy
> **Обратный прокси** — это сервер, который стоит между клиентом и бэкендом. Он принимает запросы и пересылает их на нужный сервис.

## Ключевые понятия темы

- **Reverse Proxy**: сервер, который перенаправляет запросы клиентов на один или несколько бэкенд-серверов.
- **proxy_pass**: основная директива Nginx для перенаправления трафика.
- **Location**: блок в конфиге, определяющий, как обрабатывать конкретный URL-путь.
- **Upstream**: группа серверов, между которыми Nginx может балансировать нагрузку

## Ключевые директивы

- `proxy_pass` — адрес, куда переслать запрос.
- `proxy_set_header` — передать оригинальные заголовки клиента бэкенду.
- `upstream` — описание группы серверов для балансировки.

> [!example] Пример простого прокси
> ```nginx
> location /app {
>     proxy_pass http://127.0.0.1:3000;
>     proxy_set_header Host $host;
> }
> ```

---

# Практика (минимальный рабочий пример)

Давай настроим Nginx так, чтобы при обращении к нему он "проксировал" запрос на какой-нибудь внешний сайт (эмуляция бэкенда). Отредактируй стандартный конфиг `/etc/nginx/sites-available/default` (через `sudo nano`):

```terminal
server {  
listen 80;  
server_name myserver.local;
	location /google {
    proxy_pass http://google.com/;
	}
}
```


Чистим конфиг полностью и пишем это:

```terminal
server {

    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html;

    index index.html index.htm index.nginx-debian.html;

    server_name _;

    location / {
        try_files $uri $uri/ =404;
    }

    location /google {
        proxy_pass http://google.com/;

        proxy_set_header Host google.com;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
````

---

# Задание

**Цель**: Настроить Nginx как прокси-сервер.

**Шаги**:

1. Создать новый конфигурационный файл `/etc/nginx/sites-available/test_proxy`.
2. Настроить его так, чтобы при обращении на корень `/` он перенаправлял трафик на `http://ifconfig.me`.
3. Создать символьную ссылку для активации конфига:
```
sudo ln -s /etc/nginx/sites-available/test_proxy /etc/nginx/sites-enabled/
```
4. Удалить или отключить дефолтный конфиг, чтобы не было конфликтов.
5. Проверить конфигурацию:
```
sudo nginx -t
```
6. Перезапустить nginx:
```
sudo systemctl restart nginx
```

---

# Конфигурация Reverse Proxy

```nginx
server {

    listen 80;

    server_name _;

    location / {

        proxy_pass http://ifconfig.me/;

        proxy_set_header Host ifconfig.me;
        proxy_set_header X-Real-IP $remote_addr;

    }

}
```

---

# Результат выполнения

После настройки nginx начал работать как **reverse proxy**.

Теперь схема работы выглядит так:

```
Клиент → Nginx → ifconfig.me
```

Когда клиент отправляет запрос:

```
curl localhost
```

Nginx:

1. принимает HTTP-запрос
    
2. перенаправляет его на сервис **ifconfig.me**
    
3. получает ответ от сервиса
    
4. возвращает этот ответ клиенту
    

Сервис **ifconfig.me** возвращает **внешний IP сервера**, поэтому в ответе отображается внешний IP машины.

---

# Проверка работы

Проверка выполнялась с помощью `curl`.

```bash
curl localhost
```

Ожидаемый результат — вывод внешнего IP адреса.

Также можно проверить заголовки:

```bash
curl -I localhost
```

Это позволяет убедиться, что запрос проходит через **Nginx reverse proxy**.

---

# Что мы сделали на практике

В ходе задания были выполнены следующие действия:

1. Создан отдельный конфигурационный файл сайта `test_proxy`.
    
2. Настроен блок `server` для обработки HTTP-запросов.
    
3. В блоке `location /` настроен **reverse proxy** через директиву `proxy_pass`.
    
4. Переданы оригинальные заголовки клиента (`Host`, `X-Real-IP`).
    
5. Активирован конфиг через **символическую ссылку** в `sites-enabled`.
    
6. Проверена корректность конфигурации (`nginx -t`).
    
7. Перезапущен nginx для применения настроек.
    
8. Проверена работа прокси через `curl`.
    

---

# Контрольные вопросы

1. Какая команда Nginx позволяет проверить синтаксис конфигурационных файлов без перезапуска сервиса?
    
2. В какой директории в Ubuntu принято хранить **активные** (включенные) конфигурации сайтов Nginx?
    
3. Зачем использовать Nginx перед приложением, если приложение само умеет слушать порт (например, на 3000 порту)?
    

---

### Чек-лист
- [ ] Конфиг проверен через `nginx -t`.
- [ ] Создана ссылка в `sites-enabled`.
- [ ] Сервис перезапущен через `systemctl`.
---

**Связанные заметки**  
[[Networking-Basics-for-DevOps]]  
[[HTTP Headers]]  
[[Практическая работа с логами(Nginx)]]  
[[Пакетный менеджер и Веб-сервер (Nginx)]]

**Tags**:  
#networks #nginx #proxy #devops
